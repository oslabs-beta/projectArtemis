import React from 'https://esm.sh/react';

const MenuBar = () => {
  return (
      <div>
        <button>H</button>
        <img />
        <a href="#">Docs</a>
      </div>
  );
}

export default MenuBar;
