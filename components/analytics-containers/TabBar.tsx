import React from 'https://esm.sh/react';

const TabBar = () => {

  return (
    <div className='container-tab'>
     I am the index 1
    </div>

);
}

export default TabBar;


//bring in useState √
//connect forward button to state √
//make sure state updates/incriments when button is clicked √
//make all other components display said graph
//connect each component to a number so when it gets incrimentd we can render
//the new component